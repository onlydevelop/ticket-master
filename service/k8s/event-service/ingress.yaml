apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: event-service
  namespace: ticket-master
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /events
            pathType: Prefix
            backend:
              service:
                name: event-service
                port:
                  number: 8080
